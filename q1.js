const b = 16;    
const h = 55; 

if (b > 0 && h > 0) {
    const area = 0.5 * b * h ;
    console.log("The area of the triangle is: " + area);
} else {
    console.log("Enter value of Base and height which are positive real numbers");
}
